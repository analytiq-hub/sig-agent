# NPM Configuration for @docrouter/sdk

# Ensure scoped packages are published as public
@docrouter:registry=https://registry.npmjs.org/

# Prevent accidental publishing of private packages
access=public

# Use npm registry
registry=https://registry.npmjs.org/

# Save exact versions (optional - helps with reproducible builds)
save-exact=true
